## 📁 Структура проекта

Проект следует методологии **Feature-Sliced Design (FSD)** с четким разделением на слои и сегменты.

### 🏗️ Слои архитектуры


```
src/
├── app/                    # Инициализация приложения
├── pages/                  # Страницы приложения  
├── widgets/                # Независимые виджеты
├── features/               # Бизнес-фичи пользователя
├── entities/               # Бизнес-сущности
└── shared/                 # Переиспользуемый код
```

### 🔄 Правила импортов

Строгая иерархия импортов между слоями:

- **`app`** → может импортировать из всех слоев
    
- **`pages`** → `widgets`, `features`, `entities`, `shared`
    
- **`widgets`** → `features`, `entities`, `shared`
    
- **`features`** → `entities`, `shared`
    
- **`entities`** → `shared`
    
- **`shared`** → ❌ не может импортировать из других слоев

## 📂 Структура файлов внутри слоев

### Для слоев `entities`, `features`, `widgets`, `pages`:

```
{slice-name}/               # Название слайса (kebab-case)
├── index.ts                # Barrel file (реэкспорт)
├── {kebab-case}.tsx        # Основной компонент
├── {kebab-case}.module.scss # Стили компонента
├── ui/                     # UI компоненты
├── api/                    # API запросы
├── model/                  # Бизнес-логика
├── lib/                    # Вспомогательные функции
├── types/                  # Типы TypeScript
```

### Для слоя `shared`:

```
shared/
├── ui/                     # Базовые UI компоненты
├── api/                    # Базовые API утилиты
├── model/                  # Базовые модели
├── lib/                    # Вспомогательные функции
└── types/                  # Общие типы
```

### Для слоя `app`:

```
app/
├── app.tsx                 # Главный компонент приложения
├── ui/                     # UI специфичный для app
├── api/                    # API конфигурация
├── model/                  # Глобальное состояние
├── lib/                    # Утилиты инициализации
└── types/                  # Глобальные типы
```

## 🎯 Сегменты внутри слайсов

Каждый слайс может содержать следующие сегменты:

### `ui/` - Презентационные компоненты

    
- Не содержат сложной бизнес-логики
    

### `api/` - Работа с API

- Запросы к серверу
    
- API endpoints
    
- HTTP клиенты
    

### `model/` - Бизнес-логика

- Store (Mobx)
    
- Бизнес-процессы
    

### `lib/` - Вспомогательные функции

- Утилиты
    
- Хелперы
    
- Константы
    

### `types/` - Типизация

- TypeScript типы
    
- Интерфейсы
    
- Enums
    

## 📝 Соглашения по именованию

### Файлы:

- **kebab-case**: `user-profile.tsx`, `api-client.ts`
    
- **Компоненты**: `{component-name}.tsx`
    
- **Стили**: `{component-name}.module.scss`
    

### Папки:

- **kebab-case**: `user-profile`, `api-endpoints`
    

### Типы TypeScript:

- **PascalCase** с префиксом `T`: `TUserProfile`, `TApiResponse`
    
- **Enums**: `UPPER_CASE` с префиксом `E_`: `E_USER_ROLE`
    


## 🎨 Стилизация

- **CSS Modules**: `{component-name}.module.scss`
    
- **Именование**: Соответствует имени компонента
    


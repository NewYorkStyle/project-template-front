## Definition of Done

## Quick rules

- Перед завершением работ проверь этот список.
- Если затронута логика — добавь/обнови тесты (см. [testing.md](./testing.md)).
- Если затронуты токены — перегенерируй SCSS (см. [tools.md](./tools.md)).

## Как использовать AI

- Используй DoD как финальный “gate”: по каждому пункту должно быть “да” или понятная причина “нет”.

- Импорты соответствуют FSD и публичному API.
- Новые/изменённые слайсы имеют нужные `index.ts`.
- Стили через modules / согласованную глобальную схему; токены из `design-tokens` при изменении темы.
- Для логики — unit-тесты; для сценариев — E2E через page objects и моки при необходимости.
- После изменения токенов: `npm run generate:tokens` (если правили `design-tokens.ts`).
- `npm run lint` / проверка IDE без новых ошибок в затронутых файлах.

## Чек-лист

- [ ] FSD/import zones соблюдены.
- [ ] Public API (`index.ts`) в порядке.
- [ ] API использует Orval hooks (см. [api.md](./api.md)).
- [ ] Тесты обновлены при изменении поведения.
- [ ] Токены перегенерированы при необходимости.


const AUTH = {
  PASSWORD: 'password',
  SIGN_IN: 'sign-in',
  USER_NAME: 'user-name',
};

const PROFILE = {
  TEST: 'test',
};

const COMMON = {
  TEST1: 'test1',
};

export const TEST_IDS = {
  AUTH,
  COMMON,
  PROFILE,
};
